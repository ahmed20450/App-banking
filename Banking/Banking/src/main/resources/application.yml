spring:
  application:
    name: Banking
  datasource:
    url: jdbc:postgresql://localhost:5432/banking
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
springdoc:
  default-produces-media-type: application/json
